- ssh bob@devapp01 (bob user name that is present in the server we are trying to connect, and devapp01 is the hostname of that server, instead of hostname we can also use the ipadrress of the server)

- sudo bash (open root shell to have the power of super shell, and avoid repeating password requirement)

- sudo apt install iptables -y (install ip tables)

- exit (to exit from the server)

- sudo iptables -L (view the inbound and outbound iptables rules)

- iptables -A INPUT -p tcp -s 172.16.238.187 --dport 22 -j ACCEPT (adding inbound rules -A INPUT means adding inbound rules, -s for souce ip address which wants to make request, --dport for destination port which is current server port where the request will come, -j Allowing traffic)

- iptables -A INPUT -j DROP (drops all the request)

- here the incoming request works like this. the request comes, it checks the ip tables rules from top to bottom, to check wheater this request is allowed or not. if it finds a match first time it does not check for anyting below. so in the drop rules since its meant for all kind of request it will block any request)

- iptables -A OUTPUT -p tcp -d 172.16.238.11 --dport 5432 -j ACCEPT (adding the outbound rules for postgray sql, similar as inbound)

- iptables -I OUTPUT 1 -p tcp -d google.com --dport 443 -j ACCEPT (Inserting at the top of the table. row 1)

- sudo chsh -s /bin/sh bob (change the shell from bash to bronue shell for the user bob)

- grep bob /etc/passwd (verify change)

- echo $TERM (seeing the terminal environment variable)

- echo 'export PROJECT="MERCURY"' >> ~/.profile (inside the user home/bob directory the .profile contains variables, alias, command shortcut etc, so when a particular user terminal is opened. thse .profile file is automatically executed. here we export a variable and append it at the end of .profile)

- echo 'alias up=uptime' >> .profile (make a command shorter)

- export PS1="\u@\h \$(date +%Y-%m-%d): " (update the prompt where user will show date along with name)

- uname -r (see the kernel version)

- lsblk -d (see how many disk are available)

- ps -p 1 (see the first process that start while bootng)

- sudo systemctl get-default (after the first process : systemd starts, it starts the default targer process like graphical.target)

- sudo systemctl set-default multi-user.target (changing the default target process from graphical to multi-user)

- sudo file /root/firefox.deb (shows what type of file is it)

- to install a third party software, it should be installed in opt folder

- lspci | grep -i ethernet (it shows the vendor or the company that made the ethernet interface card - NIC)

- wget https://example.com/file.txt (wget is like a download button from the internet)

- rpm is Red hat package manager which can install, remove software that is downloaded in the computer, and it also know what are the software that installed, its a low level tool, you manually installed package with rpm, rpm doesn't handle dependencies packages, it focus only on specified packages only)

- which wget (to see where this wget package file is located)

- rpm -qf /usr/bin/wget (it shows the package file for the wget)

- sudo rpm -ivh packagename (install that package, lets say for firefox there are packages, to install the firefox we have to install its packages, here -i means install, -v means verbose, -h means shows the log)

- YUM (Yellowdog Updater Modified) is a high-level package manager that uses RPM underneath. It automatically: Resolves dependencies, Downloads packages from repositories, Installs everything needed.

- sudo yum install firefox -y (install firefox software from the internet, automatically download all the packages that it needs from the internet, unlike the rpm the packages doesn't have to download the package manually first)

- yum repolist (list all the online repository for package)

- repository configuration files are stored in /etc

- apt search chromium (apt use in ubuntu/Debian instead of yum, it search the package)

- apt remove firefox (removes firefox)

- there are 3 operation mode in vi editor : Command mode, Insert mode, Last line

- yy - copy a line, dd- delete, p - paste, x - delete a single character in vim editor, u - undo the change, ctrl+r - redo the change.



-------- DNS --------

- cat /etc/resolv.conf (shows the ip address of dns server which is currently using in this system under the nameserver) its configuration file

- to change the dns server we just need to change the ip address(ip address of another dns server) of nameserver in the resolv.conf file.

- etc/nsswitch.conf (here we can find the sequence of dns lookup/resolution methon. whether file first or dns server first)


------ Networking -----

- ip a (shows all the ip addresses are asigned)
- from there in the eth0 after the inet the primary ip address of this pc will be found)
- Network Interface : here eth0 is the name of the network interface of the primary ip address, A network interface is just how your computer connects to the network (like Wi-Fi or cable)
- Gateway (its like a main gate of your house. from my phone i send a messege it goes to router. router actually decide where to send this data. here router is the gateway. it has ip address)
- ip route (shows the gateways)
- telnet (is used to connect a server to a specific port)
- telnet servername/hostname 80 (connect to the server in a http port)
- ping hostname/servername
- ip link (shows the status of network interfaces, UP or DOWN)
- sudo ip link set eth0 up (up the eth0 interface)
- sudo ip route add default via 172.16.238.1 (add a default gateway through network interface eth0, the ip is the gateway/router ip address)


------ User Management ------

- id bob (shows the user details, which group he belongs)
- sudo grep bob /etc/sudoers (shows what level of access or power bob user has)
- sudo grep bob /etc/shadow (shows the encrypted password of the the user bob)
- sudo grep chris /etc/passwd (shows the full name of the user cris)
- sudo grep mercury /etc/group (shows the group name)
- sudo useradd sarah, sudo passwd sarah (create a new user)
- sudo groupadd -g 1010 john (create a new group with group id 1010 called john)
- sudo useradd -u 1010 -g 1010 -s /bin/sh john (create the user with user id 1010, inside the group of group id : 1010, -s login shell with minimal control instead of /bin/bash)


------ Permission --------

- ls -ld sports (see the files permisson)
- first set of rwx is the owners read, write and execution command.
- second set of rwx is the groups read, write and execution command.
- thired set of rwx is for others.
- chmod 755 soccer (giving the owner full permission, and group and other has only read and execution permission)
- sudo chown -R mercury soccer (changing the ownership of the file soccer to mercury user, -R if there is multiple folders and files inside this folder)


----- SSH ---------

- ssh service use port 22 by default
- Instead of logging into a server using a password, SSH keys provide a much more secure and efficient way.
- ssh-keygen -t rsa (generate a key-pair consisting of public key and private key on your pc)
- ssh-copy-id bob@devapp01 (copy the public key to the remote server, when we are connecting to the server we need to have the private key in local and public key has to be on remote server)
- scp (secure copy protocol, it is used to transfer files between machines securely over SSH.)


----- Cronjob --------

- cronjob is like a scheduler, specific task run automatically at specific times of interval everyday or someday.
- crontab -i (prompt before deleting user's cronjob)
- crontab -r (delete user's crontab)
- crontab -l (list user's crontab)
- crontab -e (edit user's crontab)
- for inspecting the cronjobs first calumn is minutes, second for hours, the fifth is the day of the week, third is for day number of the month, fourth is for month number of the year
- 00,30 * * * * /usr/local/bin/system-debugger.sh (run every 30 minutes)


----- Systemd -------

- systemctl start servicename (start a service)
- If you want your server to act as a web server, the httpd (Apache) service must be running.
- we have to enable httpd service so that it starts automatically when system boots up and we dont need to manually start the service.
- systemctl enable httpd (enable a service for enabling it for multiuser after reboot)
- sudo systemctl stop httpd
- sudo systemctl disable httpd
- sudo systemctl status servicename
- sudo journalctl -u sample.service (see the details status of the service)
- sudo vim /etc/systemd/system/sample.service (open this systemd service)
- set the ExecStart for missing strating service, ExecStart=/bin/bash /root/sample_script.sh
- set the Restart:always for whenever the service stops for any reason it automatically starts
- sudo systemctl daemon-reload (see the changes in the service configuration and reload to start)



------ Storage --------

- Master Boot Record (MBR) is an old partitioning system used to organize data on a hard disk.
- using the MBR two types of partitions can be done. one is primary, another is extended. the MBR allows max 4 partitions. 4 primary, 3 primary 1 extended. inside the primary the os is stored. and inside the extended there can be many partition made like D:, E:, F:
- sudo gdisk /dev/vdc (creating a partition)